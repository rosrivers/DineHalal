//
//  ContentView.swift
//  Dine Halal
//  Created by Joanne on 3/5/25.

import SwiftUI
import GoogleSignIn
import FirebaseAuth
import Firebase
import SwiftUI



struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
